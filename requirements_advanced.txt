# Core
numpy
opencv-python
Pillow

# OCR + translation
paddlepaddle-gpu
# Keep PaddleOCR on the legacy 2.x API. PaddleOCR 3.x pulls PaddleX/ModelScope,
# which imports torch at module import time and can fail on CUDA/NCCL mismatches
# before OCR is even initialized.
paddleocr>=2.8.0,<3.0.0
deep-translator

# Optional: typography clustering
scikit-learn

# Optional: SAM-2 (install from source)
# git clone https://github.com/facebookresearch/segment-anything-2.git
# cd segment-anything-2
# pip install -e .

# Optional: LaMa inpainting engine
lama-cleaner
